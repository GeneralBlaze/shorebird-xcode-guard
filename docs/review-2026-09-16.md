# Code review — 2026-09-16

| | |
| --- | --- |
| HEAD | `c783eacad741c39d975c7102dcfc09e437797a23` |
| Reviewer | `deepseek-v4-flash` via provider `agent-router-oai` (OpenCode 1.18.31) |
| Job | `job_08575b17dcc3461eaa4f5bac5b514cdb` |
| Session | `ses_f556f61a5ffeckK5Qr8p7Kzydb` |
| DeepSeek verdict | **APPROVE-WITH-FIXES** — CRITICAL 0, HIGH 0, MEDIUM 4, LOW 8 |
| Orchestrator verdict | APPROVE-WITH-FIXES (agrees) |

## CRITICAL / HIGH

None reported. Nothing to confirm or reject.

## MEDIUM — spot-verified by the orchestrator

| # | File | Claim | Status |
| --- | --- | --- | --- |
| M1 | `src/util/exec.ts:70` | `execFile` can throw synchronously (empty command) and the rejection crosses the service boundary instead of a typed `spawn-error`. | CONFIRMED — reproduced: `createExec()({command:'', …})` rejects with `ERR_INVALID_ARG_VALUE`. |
| M2 | `src/services/shorebirdService.ts:116`, `src/services/xcodeService.ts:87` | No production caller passes a `CancellationToken`; cancellation is only exercised by tests. | CONFIRMED — `grep token src/{services,commands,guardController,extension}` returns nothing. |
| M3 | `src/guardController.ts:57` | `workspace.fs.readFile` inside `loadApps` has no catch, and `refreshAll()` is fire-and-forget, so an unreadable `shorebird.yaml` becomes an unhandled rejection. | CONFIRMED — lines 54–62 have no try/catch; `extension.ts` calls `void refreshWorkspace(...)`. |
| M4 | `.vscodeignore:7` / `package.json` | No `icon.png` or `galleryBanner` although §10 lists both. | CONFIRMED — `icon.png` absent, no `"icon"`/`galleryBanner` keys; already tracked in `docs/media/TODO.md`. |

## LOW — reported by DeepSeek, not independently verified

1. `src/commands/checkNow.ts:35` — drift warning re-shown without consulting `isDismissed` (§6 anti-annoyance rule).
2. `src/services/ledgerService.ts:93` — no v1→v2 schema migration stub or test (§9.1).
3. Four functions exceed 50 lines: `createGuardController` (107), `createXcodeService` (62), `createShorebirdService` (61), `activate` (57). No file exceeds 300 lines.
4. `package.json` — `npm run test:unit -- --coverage` fails because the script already passes `--coverage`.
5. `test/services/shorebirdService.test.ts:76` and `xcodeService.test.ts:175` — assertions on fake call records / captured log lines rather than returned results.
6. `docs/assumptions.md:39` — §3.3 remains `[unverified — requires a real drift build]`; §14 DoD item not fully met.
7. `src/guardController.ts:80` — `path.resolve` accepts an absolute `shorebirdGuard.ledgerPath`, allowing a workspace setting to point the ledger outside the app root.
8. `src/config.ts:39` — `shorebirdGuard.trace` is parsed but never applied.

## Categories reported clean

Rule violations (comments, `console.log`, `vscode` imports under `domain/`/`util/`, `let`), spec drift (activation event, untrusted workspaces, §8 commands + gating, §7 settings scope, no v1.1/v1.2 features), security (sudo via visible terminal, `execFile` without shell), packaging (`.vscodeignore`, VSIX 15.83 KB, 0 runtime dependencies / 14 devDependencies).

Coverage reported by the reviewer: `src/domain` 100 % lines, overall 93.1 % lines (matches the local Vitest run).

## Fixes applied (same day, follow-up session)

| # | Fix | Commit scope |
| --- | --- | --- |
| M1 | `execFile` wrapped in try/catch; synchronous throws now resolve to `{ kind: 'spawn-error' }`. Test: `exec.test.ts` "returns spawn-error instead of rejecting when the command is empty". | `src/util/exec.ts` |
| M2 | `ShorebirdOptions.token` and `XcodeService.active(timeoutMs, token)` added; the controller creates one `CancellationSource` per check, cancels a superseded in-flight check for the same app, and cancels everything on dispose. Tests: cancellation cases in `shorebirdService.test.ts` and `xcodeService.test.ts`. | services, `guardController.ts`, `extension.ts` |
| M3 | Per-file `shorebird.yaml` reads are caught and logged; `refreshAll`/`refreshBar` rejections are caught and logged. | `guardController.ts`, `extension.ts` |
| M4 | `icon.png` (128×128, generated) plus `"icon"` and `galleryBanner` in `package.json`. | manifest |
| LOW 3 | `createGuardController`, `createXcodeService`, `createShorebirdService`, `activate` split into helpers; no function in `src/` exceeds 50 lines. | services, `extension.ts` |

Post-fix: 107 unit tests, 16 integration tests, `domain/` 100 %, overall 93.5 %, VSIX 17.1 KB.

Not addressed: LOW 1 (`checkNow` re-shows the warning — treated as intended, since the user explicitly asked for a check), LOW 2 (v1→v2 migration stub), LOW 4 (`-- --coverage` double flag), LOW 5 (test assertions on the exec fake), LOW 6 (§3.3 unverified), LOW 7 (absolute `ledgerPath` escapes the app root), LOW 8 (`trace` setting unused).

## Caveats

- The reviewer's `npx vsce package` triggered `vscode:prepublish`, which rebuilt `dist/extension.js` (gitignored). `git status --porcelain` was empty before and after the review; no tracked file changed.
- One permission prompt was approved: reading `/Users/princewill/Downloads/shorebird-xcode-guard-spec.md` (read-only, on the allow-list).
- The reply arrived in the required SEVERITY/FILE/CLAIM/EVIDENCE/FIX format with a VERDICT line on the first attempt; no reformat round was needed.
- No fixes were applied in this session.
